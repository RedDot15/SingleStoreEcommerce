server:
  port: 3000
  servlet:
    context-path: /store

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/single_store"
    username: root
    password: quangminh1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  config:
    import: env.properties
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB

cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    region:
      static: ap-southeast-1
    stack:
      auto: false

application:
  cloudfront:
    name: dyg4oa4f8q8zk
  bucket:
    name: mysinglestore3

jwt:
  signer-key: ${SIGNER_KEY}